# 🛠️ 개발환경 정보

> Academy Manager 프로젝트의 개발환경 구성 정보입니다.

---

## 📌 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | Academy Manager |
| **설명** | 학원 운영에 필요한 학생/수업/출결/수납/강사 관리를 통합 제공하는 웹 기반 학원 관리 시스템 |
| **개발 형태** | 개인 프로젝트 |
| **개발 환경** | Antigravity Editor |

---

## 🔧 기술 스택

### Backend
| 기술 | 버전/설명 |
|------|-----------|
| Python | 3.x |
| Django | 4.x (권장: 4.2 LTS) |
| PostgreSQL | 데이터베이스 |

### Frontend
| 기술 | 설명 |
|------|------|
| Django Template | 서버 사이드 렌더링 |
| Bootstrap 5 | UI 프레임워크 (Dark Mode 지원) |
| Chart.js | 차트/그래프 라이브러리 |
| FullCalendar | 캘린더 UI 라이브러리 |

### DevOps / Infrastructure
| 기술 | 설명 |
|------|------|
| Docker | 컨테이너화 |
| Docker Compose | 다중 컨테이너 오케스트레이션 |
| Git / GitHub | 버전 관리 |

### 확장 구성 (권장)
| 기술 | 용도 |
|------|------|
| Celery | 비동기 작업 처리 |
| Redis | Celery 브로커/캐시 |
| Django Channels | 실시간 기능 (선택) |
| WeasyPrint / wkhtmltopdf | PDF 생성 (선택) |

---

## 📁 프로젝트 구조

```
academy_manager/
├── accounts/          # 계정/권한/로그인
├── students/          # 학생 관리
├── classes/           # 반/수업 관리
├── attendance/        # 출결 관리
├── payments/          # 수납 관리
├── teachers/          # 강사 관리
├── dashboard/         # 대시보드
├── schedule/          # 시간표/캘린더
├── notifications/     # 알림 발송
├── academics/         # 성적/학습 관리
├── parent_portal/     # 학부모 포털
├── messaging/         # 메시지
├── exports/           # 데이터 내보내기
├── core/              # 공통 모듈
├── templates/         # HTML 템플릿
├── static/            # 정적 파일
├── config/            # 프로젝트 설정
└── manage.py          # Django 관리 스크립트
```

---

## 🐳 Docker 설정

### 서비스 구성
| 서비스 | 설명 |
|--------|------|
| web | Django 애플리케이션 |
| db | PostgreSQL 데이터베이스 |
| redis | Celery 브로커/캐시 (선택) |
| worker | Celery worker (선택) |
| beat | Celery beat 스케줄러 (선택) |

### 주요 환경변수 (.env)
```env
DB_HOST=db
DB_NAME=academy_db
DB_USER=postgres
DB_PASSWORD=your_password
SECRET_KEY=your_secret_key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
REDIS_URL=redis://redis:6379/0
TIME_ZONE=Asia/Seoul
```

---

## 🚀 시작하기

### 1. 프로젝트 클론
```bash
git clone https://github.com/ghwls3644-sketch/academy-manager.git
cd academy-manager
```

### 2. 환경변수 설정
```bash
# .env.example을 복사하여 .env 생성
cp .env.example .env
# .env 파일 수정 (DB_HOST=db 확인)
```

### 3. Docker 실행
```bash
docker compose up --build
```

### 4. 데이터베이스 마이그레이션
```bash
docker compose exec web python manage.py migrate
```

### 5. 슈퍼유저 생성
```bash
docker compose exec web python manage.py createsuperuser
```

### 6. 브라우저 접속
- http://localhost:8000

---

## 📋 자주 사용하는 명령어

| 작업 | 명령어 |
|------|--------|
| 서버 시작 | `docker compose up` |
| 서버 시작 (백그라운드) | `docker compose up -d` |
| 서버 중지 | `docker compose down` |
| 마이그레이션 생성 | `docker compose exec web python manage.py makemigrations` |
| 마이그레이션 적용 | `docker compose exec web python manage.py migrate` |
| Django 쉘 | `docker compose exec web python manage.py shell` |
| 슈퍼유저 생성 | `docker compose exec web python manage.py createsuperuser` |
| 이미지 재빌드 | `docker compose build` |
| 컨테이너 상태 확인 | `docker compose ps` |
| 로그 확인 | `docker compose logs -f` |

---

## 📦 주요 패키지 (requirements.txt)

```txt
# 핵심
Django>=4.2
psycopg2-binary
python-dotenv

# 비동기/스케줄
celery
redis

# QR 생성
qrcode
Pillow

# Excel 내보내기
openpyxl

# PDF 내보내기
reportlab

# HTTP 호출
requests
```

---

## 🔗 참고 문서

- [Docker개발가이드.md](./Docker개발가이드.md) - Docker 환경 상세 가이드
- [프로젝트계획서.md](./프로젝트계획서.md) - 프로젝트 개요
- [추가기능계획서.md](./추가기능계획서.md) - 기능 확장 계획
- [추가기능계획서2.md](./추가기능계획서2.md) - 기능 추가 상세
- [추가기능계획서3.md](./추가기능계획서3.md) - 전체 로드맵
- [UX개선기능.md](./UX개선기능.md) - UX 개선 계획

---

*마지막 업데이트: 2025-12-19*
