from pathlib import Path

content = """# 계획서.md — 추가 기능 구현을 위한 설치/연동 정리

본 문서는 **학원 관리 프로그램**에 아래 기능들을 추가할 때, 보통 필요한 **설치 요소(패키지/서비스/도구)**와 **권장 구성**을 한 번에 정리한 문서입니다.  
(Django + PostgreSQL만으로도 구현은 가능하지만, 자동화/발송/리포트/백업은 확장 구성 시 운영이 훨씬 안정적입니다.)

---

## 1. 권장 “기본 확장 세트” (대부분의 기능에 공통으로 도움)

### 1) 작업 큐/스케줄러 (자동 알림, 백업, 집계에 사실상 필수급)
- **Redis**: Celery 브로커/캐시로 사용
- **Celery + Celery Beat**: 비동기 작업(예약/재시도/실패처리) + 정기 작업(월별 집계/백업)

### 2) 외부 발송 서비스(커뮤니케이션)
- **SMS 발송 서비스**: 네이버 SENS / Twilio / Nurigo(쿨SMS) 등 → **API 키 필요**
- **카카오 알림톡**: 비즈메시지(공식 대행사) 통해 **템플릿 승인 + API 사용**

---

## 2. 기능별 설치/연동 체크리스트

### 📱 2.1 커뮤니케이션 기능 (SMS/카카오톡, 공지, 상담일지)
- (필수) **발송 서비스 가입 + API 키**
- (권장 pip) `requests` (또는 업체 SDK)
- (권장) **Celery + Redis**
  - 예약 발송, 자동 발송, 재시도, 실패 처리(예: “발송 실패 로그 저장 후 재발송”)

**구현 시 꼭 같이 넣을 것(운영 안전장치)**
- 발송 **템플릿 관리**
- 발송 **미리보기**
- 발송 **로그/상태(성공/실패/재시도/수신거부) 기록**

---

### 📈 2.2 강화된 분석/리포트 (출석률 트렌드, 수익 대시보드, 성장 리포트)
- (프론트) Chart.js / ECharts 등 **JS 차트 라이브러리** (CDN 사용 가능)
- (권장) Redis(캐시), Celery Beat(월/분기 집계 사전 계산)

**추천 지표(실무형)**
- 반/강사/시간대별 출석률
- 재등록률(유지율)
- 연체율/미수금 규모
- 과목/반/지점/결제수단별 매출

---

### 📅 2.3 스케줄 관리 (시간표/캘린더, 보강, 휴원)
- (프론트) FullCalendar 등 캘린더 UI 라이브러리 (CDN 가능)
- (선택 pip) `icalendar` (ICS 내보내기/공유가 필요할 때)

**운영 핵심**
- 강사/강의실/정원 **충돌 감지** (겹치면 저장 차단 또는 경고)

---

### 📝 2.4 학습 관리(LMS) (과제/성적/자료)
- (필수) Django 기본 업로드/권한만으로도 가능
- (선택) 대용량 파일/트래픽이 크면 S3/MinIO 같은 스토리지 고려

**초기 MVP 추천**
- 과제: 등록/제출/미제출 알림
- 자료: 권한별 다운로드 제한 + 만료 기간

---

### 🔐 2.5 운영 편의성 (QR 출석, 내보내기, 백업/복원)
#### QR 출석체크
- (서버 pip) `qrcode` + `Pillow` (QR 생성)
- (프론트 선택) `html5-qrcode` (웹에서 카메라로 스캔할 때)

**권장 안전장치**
- 시간 제한 QR(토큰 만료), 회차별 토큰, 대리출석 방지 로직

#### 데이터 내보내기(Excel/PDF)
- Excel: (pip) `openpyxl` (또는 `pandas`)
- PDF: 아래 중 **하나 선택**
  - **ReportLab**: (pip만으로) PDF 생성 가능, 레이아웃은 직접 구성
  - **wkhtmltopdf**: 시스템 바이너리 설치 필요(HTML→PDF)
  - **WeasyPrint**: 리눅스 의존성 설치 필요(HTML→PDF)

#### 백업/복원
- DB 백업/복원: `pg_dump` / `pg_restore` (Postgres 컨테이너에 보통 포함)
- 정기 자동화: Celery Beat 또는 cron
- 파일(업로드 자료)까지 백업 시:
  - (선택) S3/MinIO + 백업 도구(rclone 등)

---

### 💰 2.6 수납 기능 강화 (자동 알림, 할인/장학, 영수증)
- (권장) Celery Beat로 “납부일 N일 전 자동 알림” 스케줄
- (권장) 환불/정산 규정 자동 계산 + 승인 흐름(권한)

**추가로 추천(분쟁 예방/회계 편의)**
- 미수금/연체 단계 관리(1차/2차/최종)
- 환불 규정(일할/횟수) 자동 계산
- 매출 분류(반/강사/과목/지점/결제수단)

---

## 3. 보안/권한/감사 로그(운영 필수급 추천)

### 권한/보안
- 역할 기반 권한(RBAC): 원장/실장/강사/데스크/회계 등
- 2단계 인증(선택): `django-two-factor-auth` (내부적으로 otp 사용 가능)

### 감사 로그/변경 이력
- 변경 이력: `django-simple-history`
- 감사 로그: `django-auditlog` (팀 취향에 따라 택1)

### 객체 단위 권한(필요할 때만)
- `django-guardian` (예: “특정 학생/반 데이터는 특정 강사만 접근” 같은 세밀한 권한)

---

## 4. requirements.txt 추천(예시)

> 실제 적용 시, **PDF 방식(ReportLab vs WeasyPrint vs wkhtmltopdf)** 과  
> **감사로그/권한**은 프로젝트 정책에 맞게 선택하세요.

```txt
# HTTP 호출(알림 발송 API)
requests

# 비동기/스케줄(자동 알림/백업/집계)
celery
redis

# QR 생성
qrcode
Pillow

# Excel 내보내기
openpyxl

# PDF 내보내기(택1)
reportlab
# 또는 WeasyPrint / wkhtmltopdf 방식 선택

# 권한/감사(선택)
django-guardian
django-simple-history
# 또는 django-auditlog

# 2FA(선택)
django-two-factor-auth
